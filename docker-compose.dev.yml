name: ai-workspace

services:
  # PostgreSQL Database
  db:
    image: pgvector/pgvector:pg17
    container_name: ai-workspace-db
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-backend}
      - POSTGRES_USER=${POSTGRES_USER:-backend}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
    volumes:
      - ai_workspace_postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:${DB_FORWARD_PORT:-5435}:5432"  # Only localhost access
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-backend}"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - ai-workspace-network

  # Redis (for token blacklist and challenge storage)
  redis:
    image: redis:8-alpine
    container_name: ai-workspace-redis
    restart: unless-stopped
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:-changeme}
    ports:
      - "127.0.0.1:${REDIS_FORWARD_PORT:-6382}:6379"  # Only localhost access
    volumes:
      - ai_workspace_redis_data:/data
    command: >
      redis-server
      --appendonly yes
      --requirepass ${REDIS_PASSWORD:-changeme}
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "sh", "-c", "redis-cli -a $${REDIS_PASSWORD:-changeme} ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - ai-workspace-network

  # Application (hot reload via volume mounts — WSL and VPS)
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    image: ai-workspace-backend:dev
    container_name: ai-workspace-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      # Secrets and app config live here (DATABASE_URL, REDIS_URL, …).
      # Do not re-list them under environment: with ${VAR} — host/root .env
      # interpolation (or defaults like changeme) would override env_file values.
      - ./backend/.env
    environment:
      # Non-secret runtime defaults only (must not shadow secrets from env_file)
      - ENVIRONMENT=${ENVIRONMENT:-development}
      - PROJECT_NAME=${PROJECT_NAME:-ai-workspace}
      - APP_PORT=${APP_PORT:-8000}
      - LOG_LEVEL=${LOG_LEVEL:-DEBUG}
    ports:
      - "127.0.0.1:${APP_PORT:-8003}:8000"  # Only localhost access (Caddy proxy)
    volumes:
      # Mount source code for hot reload (paths relative to repo root)
      # Using :delegated for better performance on WSL2/macOS
      - ./backend/app:/app/app:rw,delegated
      - ./backend/scripts:/app/scripts:rw,delegated
      - ./backend/tests:/app/tests:rw,delegated
      - ./backend/evals:/app/evals:rw,delegated
      - ./backend/migrations:/app/migrations:rw,delegated
      - ./backend/main.py:/app/main.py:rw,delegated
      - ./backend/cli:/app/cli:ro,delegated
      - ./backend/cli.py:/app/cli.py:ro,delegated
      - ./backend/pyproject.toml:/app/pyproject.toml:ro,delegated
      - ./backend/images:/app/images:ro,delegated
      # Read-only corpus for the RAG eval harness (backend/evals/rag) — the golden
      # set references these paths, so the running container needs to see them.
      - ./docs:/repo/docs:ro,delegated
      - ./CLAUDE.md:/repo/CLAUDE.md:ro,delegated
      # Mount uploads directory for persistent file storage
      - ai_workspace_uploads:/app/uploads
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - ai-workspace-network
      - rustfs-network

networks:
  ai-workspace-network:
    name: ai-workspace-network
  rustfs-network:
    name: rustfs-network
    external: true

volumes:
  ai_workspace_postgres_data:
    name: ai_workspace_postgres_data
  ai_workspace_redis_data:
    name: ai_workspace_redis_data
  ai_workspace_uploads:
    name: ai_workspace_uploads
